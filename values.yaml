flock:
  singleBinary: true

  api:
    image:
      repository: flock-api
      tag: latest
      pullPolicy: Never
    replicas: 1
    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 256Mi

  ingress:
    enabled: true
    hosts:
      - host: api.flock.local
        paths:
          - path: /
            pathType: Prefix

  apiConfig:
    log:
      level: "debug"
      format: "text"

  postgresql:
    enabled: true
    auth:
      username: flock
      password: flock
      database: flock
    primary:
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
